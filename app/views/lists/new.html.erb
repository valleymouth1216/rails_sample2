<h1>新規投稿</h1>
<%= form_with model: @list,url:lists_path, method: :post do |f| %>
<!--urlはテーブル名が入るイメージ(データをいれたい場所)-->
<!--lists_pathと'/lists'は同じ意味-->
<h4>タイトル</h4>
<%=f.text_field :title %>

<h4>本文</h4>
<%=f.text_area :body %>

<%=f.submit "投稿" %>

<% end %>